import React from "react";

const Category = ({
    _id,
    name,
    market_cap,
    market_cap_change_24h,
    volume_24h,
    updated_at,
}) => {
    return (
        <div>
            <h1>hi</h1>
        </div>
    );
}

export default Category;